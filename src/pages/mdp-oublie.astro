---
import Layout from "../layouts/Layout.astro";
import Icon from "../assets/Icon.svg";
import Button from "../components/Button.astro";
---

<Layout
    bgColor="var(--color-primary)"
>
    <div class="flex flex-col items-center justify-center mt-25 lg:mt-40 px-4 sm:px-6 py-16 sm:py-20">
        <div class="bg-white/90 w-full max-w-2xl p-8 sm:p-12 rounded-3xl shadow-[0_0_0_3px_black,6px_6px_0px_2px_black] text-center">
            <div class="mb-10 sm:mb-12 space-y-10">
                <div class="w-20 bg-primary h-20 sm:w-24 sm:h-24 mx-auto mb-8 sm:mb-10 rounded-full bg-linear-to-br from-primary to-tertiary flex items-center justify-center border-3 border-black shadow-[0_0_0_2px_black,3px_3px_0px_1px_black]">
                    <img src={Icon.src} alt="Icon">
                </div>
                
                <h1 class="text-black font-oi text-3xl sm:text-4xl md:text-5xl mb-6 sm:mb-8">
                    Mot de passe oublié ?
                </h1>
                
                <p class="text-black font-nohemi text-base sm:text-lg mb-0 max-w-lg mx-auto">
                    Pas de panique ! Pour réinitialiser votre mot de passe, contactez-nous via notre formulaire de contact.
                </p>
            </div>

            <div class="bg-pink-50 space-y-5 p-6 sm:p-8 rounded-2xl border-2 border-black mb-8 sm:mb-10">
                <h2 class="font-oi text-black text-xl sm:text-2xl mb-5 sm:mb-6">
                    Comment faire ?
                </h2>
                <ol class="text-left font-nohemi text-sm sm:text-base space-y-4 sm:space-y-5 text-gray-700">
                    <li class="flex items-start gap-3">
                        <span class="shrink-0 w-5 h-auto rounded-full bg-primary text-white flex items-center justify-center font-bold text-sm">1</span>
                        <span>Cliquez sur le bouton ci-dessous pour accéder au formulaire de contact</span>
                    </li>
                    <li class="flex items-start gap-3">
                        <span class="shrink-0 w-5 h-auto rounded-full bg-primary text-white flex items-center justify-center font-bold text-sm">2</span>
                        <span>Indiquez votre adresse email et demandez une réinitialisation de mot de passe</span>
                    </li>
                    <li class="flex items-start gap-3">
                        <span class="shrink-0 w-5 h-auto rounded-full bg-primary text-white flex items-center justify-center font-bold text-sm">3</span>
                        <span>Notre équipe vous répondra dans les plus brefs délais</span>
                    </li>
                </ol>
            </div>

            <div class="space-y-5 flex flex-col sm:space-y-6 mt-10 sm:mt-12">
                <Button bgClass="bg-primary" colorClass="text-white" text="Contacter l'équipe" page="contact" />
                <Button bgClass="bg-white" colorClass="text-primary" text="Retour à la connexion" page="connexion" />
            </div>

            <p class="text-gray-600 font-nohemi text-xs sm:text-sm mt-10 sm:mt-12">
                💡 Conseil : Assurez-vous d'utiliser l'adresse email avec laquelle vous vous êtes inscrit
            </p>
        </div>
    </div>
</Layout>

<script>
    let countdown = 10;
    const redirectButton = document.querySelector('a[href="/contact"]');
</script>
