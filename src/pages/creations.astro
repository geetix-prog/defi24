---
import Layout from "../layouts/Layout.astro";
import Rendu from "../components/Rendu.astro";
import Trophy from "../assets/trophy.svg";
import { getAllCreations } from "../../backend/backend.mjs";

const creations = await getAllCreations();

const bgColors = [
    'bg-yellow/50',
    'bg-primary/50', 
    'bg-secondary/50',
    'bg-tertiary/50',
    'bg-pink/50',
];

const pointsColors = [
    'text-yellow',
    'text-primary',
    'text-secondary',
    'text-tertiary',
    'text-pink',
];
---

<Layout
    title="Les créations | Défi 24h MMI"
    description="Découvrez toutes les identités visuelles créées en 24 heures par nos équipes talentueuses !"
    keywords="créations, identité visuelle, 24h, MMI, design, graphisme"
    bgColor="linear-gradient(to bottom right, var(--color-pink), var(--color-primary))"
>
    <main class="min-h-screen pb-20">
        <div class="container mx-auto px-4 pt-24 sm:pt-32">
            <div class="mb-8 sm:mb-12 md:mb-16 max-w-4xl mx-auto text-center">
                <div class="inline-flex items-center gap-2 bg-yellow border-2 border-black rounded-full px-4 sm:px-6 py-2 mb-6 shadow-[0_0_0_3px_black,4px_4px_0px_2px_black]">
                    <Trophy class="w-4 h-4 sm:w-5 sm:h-5" />
                    <p class="font-nohemi text-xs sm:text-sm font-bold uppercase tracking-wider">
                        Résultats officiels
                    </p>
                </div>
                
                <h1 class="font-oi text-5xl sm:text-6xl md:text-7xl lg:text-8xl text-white mb-4 sm:mb-6">
                    Les créations
                </h1>
                
                <p class="font-nohemi text-base sm:text-lg md:text-xl text-white/90">
                    Découvrez toutes les identités visuelles créées en 24 heures par nos équipes talentueuses ! 🎨
                </p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8">
                {creations.map((creation, index) => (
                    <Rendu
                        nom={creation.nom}
                        points={creation.points}
                        membres={creation.membres}
                        lien={creation.lien}
                        imageUrl={creation.logo_url}
                        bgColor={bgColors[index % bgColors.length]}
                        pointsColor={pointsColors[index % pointsColors.length]}
                        isFirst={index === 0}
                    />
                ))}
            </div>

            {creations.length === 0 && (
                <div class="text-center py-20">
                    <p class="font-nohemi text-xl text-white/80">
                        Aucune création disponible pour le moment.
                    </p>
                </div>
            )}
        </div>
    </main>
</Layout>