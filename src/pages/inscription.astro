---
import Layout from "../layouts/Layout.astro";
---

<Layout
	title="Inscription - Défi 24h MMI | Rejoignez l'aventure !"
	description="Inscrivez-vous au Défi 24h MMI ! Créez votre compte, rejoignez ou formez votre équipe et participez à 24 heures de création non-stop le 09 janvier 2026."
	keywords="inscription défi 24h, créer compte MMI, rejoindre défi 24h, s'inscrire événement créatif, inscription équipe"
	canonical="/inscription"
>
    <div class="flex flex-col space-y-15 justify-center items-center mt-25 lg:mt-40 px-4">
		<h1
			class="text-white font-oi text-center text-4xl sm:text-5xl md:text-6xl lg:text-7xl xl:text-8xl leading-tight sm:leading-tight md:leading-normal tracking-wide">
			Inscription
		</h1>
        
        <p class="text-white font-nohemi text-center text-base sm:text-lg max-w-lg mb-10">
            Rejoins l'aventure du Défi 24h ! Crée ton compte pour t'inscrire, former ton équipe et participer au challenge.
        </p>
        
        <div class="bg-white/90 w-full max-w-xl p-8 sm:p-10 rounded-3xl shadow-[0_0_0_3px_black,6px_6px_0px_2px_black]">
            <form id="registerForm" class="space-y-5">
                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="prenom" class="block font-nohemi text-black font-bold mb-2">Prénom</label>
                        <input 
                            type="text" 
                            name="prenom" 
                            id="prenom" 
                            required 
                            placeholder="Jean"
                            class="w-full px-4 py-3 border-3 border-black rounded-2xl font-nohemi text-base focus:outline-none focus:ring-3 focus:ring-primary transition-all"
                        />
                    </div>
                    <div>
                        <label for="nom" class="block font-nohemi text-black font-bold mb-2">Nom</label>
                        <input 
                            type="text" 
                            name="nom" 
                            id="nom" 
                            required 
                            placeholder="Dupont"
                            class="w-full px-4 py-3 border-3 border-black rounded-2xl font-nohemi text-base focus:outline-none focus:ring-3 focus:ring-primary transition-all"
                        />
                    </div>
                </div>

                <div>
                    <label for="email" class="block font-nohemi text-black font-bold mb-2">Adresse email</label>
                    <input 
                        type="email" 
                        name="email" 
                        id="email" 
                        required 
                        placeholder="ton-email@example.com"
                        class="w-full px-5 py-3.5 border-3 border-black rounded-2xl font-nohemi text-base focus:outline-none focus:ring-3 focus:ring-primary transition-all"
                    />
                </div>

                <div>
                    <label for="promo" class="block font-nohemi text-black font-bold mb-2">Promo</label>
                    <select 
                        name="promo" 
                        id="promo" 
                        required
                        class="w-full px-5 py-3.5 border-3 border-black rounded-2xl font-nohemi text-base focus:outline-none focus:ring-3 focus:ring-primary transition-all bg-white cursor-pointer"
                    >
                        <option value="" disabled selected>- Select -</option>
                        <option value="2025-2028">2025-2028</option>
                        <option value="2024-2027">2024-2027</option>
                        <option value="2023-2026">2023-2026</option>
                        <option value="ancien">Ancien</option>
                    </select>
                </div>

                <div>
                    <label for="avatar" class="block font-nohemi text-black font-bold mb-2">Photo de profil (optionnel)</label>
                    <input 
                        type="file" 
                        name="avatar" 
                        id="avatar" 
                        accept="image/*"
                        class="w-full px-4 py-3 border-3 border-black rounded-2xl font-nohemi text-base focus:outline-none focus:ring-3 focus:ring-primary transition-all file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-primary file:text-white hover:file:bg-opacity-90"
                    />
                </div>
                
                <div>
                    <label for="password" class="block font-nohemi text-black font-bold mb-2">Mot de passe</label>
                    <div class="relative">
                        <input 
                            type="password" 
                            name="password" 
                            id="password" 
                            required 
                            placeholder="••••••••"
                            class="w-full px-5 py-3.5 border-3 border-black rounded-2xl font-nohemi text-base focus:outline-none focus:ring-3 focus:ring-primary transition-all pr-12"
                        />
                        <button 
                            type="button" 
                            id="togglePassword"
                            class="absolute right-4 top-1/2 -translate-y-1/2 text-gray-600 hover:text-black transition"
                        >
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M2.036 12.322a1.012 1.012 0 010-.639C3.423 7.51 7.36 4.5 12 4.5c4.638 0 8.573 3.007 9.963 7.178.07.207.07.431 0 .639C20.577 16.49 16.64 19.5 12 19.5c-4.638 0-8.573-3.007-9.963-7.178z" />
                                <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                            </svg>
                        </button>
                    </div>
                </div>
                
                <div>
                    <label for="passwordConfirm" class="block font-nohemi text-black font-bold mb-2">Confirmer le mot de passe</label>
                    <div class="relative">
                        <input 
                            type="password" 
                            name="passwordConfirm" 
                            id="passwordConfirm" 
                            required 
                            placeholder="••••••••"
                            class="w-full px-5 py-3.5 border-3 border-black rounded-2xl font-nohemi text-base focus:outline-none focus:ring-3 focus:ring-primary transition-all pr-12"
                        />
                    </div>
                </div>
                
                <button 
                    type="submit"
                    class="inline-block w-full bg-tertiary text-white px-6 sm:px-8 md:px-10 py-2 sm:py-2.5 md:py-3 rounded-full font-nohemi font-black text-base sm:text-lg shadow-[0_0_0_2px_black,3px_3px_0px_1px_black] sm:shadow-[0_0_0_3px_black,4px_4px_0px_2px_black] hover:scale-105 active:scale-95 transition-transform duration-150 ease-in text-center cursor-pointer"
                >
                    Créer mon compte
                </button>
                
                <div id="message" class="text-center font-nohemi font-bold mt-4"></div>
                
                <div class="text-center mt-6 mb-4">
                    <span class="text-gray-600 font-nohemi text-sm uppercase">Déjà un compte ?</span>
                </div>
                
                <a 
                    href="/connexion"
                    class="inline-block w-full bg-white text-tertiary px-6 sm:px-8 md:px-10 py-2 sm:py-2.5 md:py-3 rounded-full font-nohemi font-black text-base sm:text-lg shadow-[0_0_0_2px_black,3px_3px_0px_1px_black] sm:shadow-[0_0_0_3px_black,4px_4px_0px_2px_black] hover:scale-105 active:scale-95 transition-transform duration-150 ease-in text-center"
                >
                    Se connecter
                </a>
            </form>
        </div>
    </div>
</Layout>

<script>
    const userDataString = localStorage.getItem('user');
    const token = localStorage.getItem('token');
    
    if (userDataString && token) {
        window.location.href = '/profile';
    }
    const form = document.getElementById('registerForm') as HTMLFormElement;
    const messageDiv = document.getElementById('message') as HTMLDivElement;
    const togglePasswordBtn = document.getElementById('togglePassword');
    const togglePasswordConfirmBtn = document.getElementById('togglePasswordConfirm');
    const passwordInput = document.getElementById('password') as HTMLInputElement;
    const passwordConfirmInput = document.getElementById('passwordConfirm') as HTMLInputElement;

    togglePasswordBtn?.addEventListener('click', () => {
        if (passwordInput.type === 'password') {
            passwordInput.type = 'text';
        } else {
            passwordInput.type = 'password';
        }
    });

    togglePasswordConfirmBtn?.addEventListener('click', () => {
        if (passwordConfirmInput.type === 'password') {
            passwordConfirmInput.type = 'text';
        } else {
            passwordConfirmInput.type = 'password';
        }
    });

    form.addEventListener('submit', async (e) => {
        e.preventDefault();
        
        const prenomInput = document.getElementById('prenom') as HTMLInputElement;
        const nomInput = document.getElementById('nom') as HTMLInputElement;
        const emailInput = document.getElementById('email') as HTMLInputElement;
        const promoInput = document.getElementById('promo') as HTMLSelectElement;
        const avatarInput = document.getElementById('avatar') as HTMLInputElement;
        
        const prenom = prenomInput.value;
        const nom = nomInput.value;
        const email = emailInput.value;
        const promo = promoInput.value;
        const password = passwordInput.value;
        const passwordConfirm = passwordConfirmInput.value;

        messageDiv.textContent = '';

        if (password.length < 8) {
            messageDiv.textContent = 'Le mot de passe doit contenir au moins 8 caractères.';
            messageDiv.style.color = 'red';
            return;
        }

        if (password !== passwordConfirm) {
            messageDiv.textContent = 'Les mots de passe ne correspondent pas.';
            messageDiv.style.color = 'red';
            return;
        }

        try {
            const formData = new FormData();
            formData.append('email', email);
            formData.append('password', password);
            formData.append('passwordConfirm', passwordConfirm);
            formData.append('nom', nom);
            formData.append('prenom', prenom);
            formData.append('promo', promo);

            if (avatarInput.files && avatarInput.files[0]) {
                formData.append('avatar', avatarInput.files[0]);
            }

            const response = await fetch('/api/register', {
                method: 'POST',
                body: formData
            });

            const data = await response.json();

            if (response.ok) {
                messageDiv.textContent = 'Inscription réussie ! Redirection...';
                messageDiv.style.color = 'green';
                setTimeout(() => {
                    window.location.href = '/connexion';
                }, 1500);
            } else {
                messageDiv.textContent = data.error || 'Erreur lors de l\'inscription';
                messageDiv.style.color = 'red';
            }
        } catch (error) {
            messageDiv.textContent = 'Erreur de connexion au serveur';
            messageDiv.style.color = 'red';
        }
    });
</script>


